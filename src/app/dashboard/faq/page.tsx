import { redirect } from "next/navigation";

/**
 * /dashboard/faq был старой версией страницы базы знаний.
 * /dashboard/knowledge — текущая полная версия (включает управление AI промптом
 * и шаблонами поверх обычного управления FAQ + документами).
 *
 * Оставляем редирект чтобы:
 * - Старые ссылки в письмах, sales-материалах, закладках клиентов не ломались.
 * - Любые внешние документы продолжали приводить пользователя на правильную страницу.
 *
 * Сама функция «база знаний / FAQ» полностью сохранена — она живёт на /knowledge.
 */
export default function FaqRedirectPage() {
  redirect("/dashboard/knowledge");
}
